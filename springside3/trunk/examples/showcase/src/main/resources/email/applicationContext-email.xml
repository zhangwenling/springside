<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd"
	default-lazy-init="true">

	<description>Email配置</description>

	<!-- 邮件发送者 -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host" value="smtp.gmail.com" />
		<property name="username" value="springside3.demo" />
		<property name="password" value="demoforyou" />
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.auth">true</prop>
				<!-- gmail smtp server必须参数 -->
				<prop key="mail.smtp.starttls.enable">true</prop>
			</props>
		</property>
	</bean>

	<!-- 群发文本邮件服务类 -->
	<bean id="simpleMailService" class="org.springside.examples.showcase.email.SimpleMailService">
		<property name="mailSender" ref="mailSender" />
		<property name="executor" ref="executor"/>
	</bean>

	<!-- MIME邮件服务类 -->
	<bean id="mimeMailService" class="org.springside.examples.showcase.email.MimeMailService">
		<property name="mailSender" ref="mailSender" />
		<property name="configuration" ref="freemarkerConfiguration"></property>
	</bean>

	<!-- 用于生成HTML邮件内容的Freemarker引擎 -->
	<bean id="freemarkerConfiguration" class="org.springframework.ui.freemarker.FreeMarkerConfigurationFactoryBean">
		<property name="templateLoaderPath" value="classpath:/email" />
	</bean>

	<!-- 用于群发邮件的线程池 -->
	<bean id="executor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
		<property name="corePoolSize" value="5" />
	</bean>
</beans>