<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<description>JMS消费者演示配置</description>

	<!-- 异步接收Queue消息Container -->
	<bean id="queueContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer" lazy-init="false">
		<property name="connectionFactory" ref="jmsFactory" />
		<property name="destination" ref="notifyQueue" />
		<property name="messageListener" ref="notifyQueueListener" />
	</bean>

	<!-- 异步接收Topic消息Container -->
	<bean id="topicContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer" lazy-init="false">
		<property name="connectionFactory" ref="jmsFactory" />
		<property name="destination" ref="notifyTopic" />
		<property name="messageListener" ref="notifyTopicListener" />
	</bean>

	<!-- 异步接收Topic消息处理类 -->
	<bean id="notifyTopicListener" class="org.springside.examples.showcase.jms.NotifyTopicListener" />

	<!-- 异步接收Topic消息处理类 -->
	<bean id="notifyQueueListener" class="org.springside.examples.showcase.jms.NotifyQueueListener" />
</beans>